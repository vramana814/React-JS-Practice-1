// Prorgram to check whether a number is "Positive" or "Negative" or "Zero"

/*import React, { Component } from 'react'

export default class Checknumber extends Component {
    state = {
        statement : 'Please Enter a Number',
        number : null
    }
    isPositive(e){ 
        this.setState({
            number : e.target.value
        })
        this.buttonHandler()
    }
    buttonHandler(){
        console.log('Button Clicked')
        if(this.state.number != null){
          if(this.state.number>0){
              return <h2></h2>
          }
        }
    }
    
  render() {
        return (
        <div className = "PositiveorNot">
            <input type = 'number' onChange = {this.isPositive.bind(this)}/>
            <p>{this.state.statement}</p>
            <button onClick = {this.buttonHandler.bind(this)}>checkNumber</button>
        </div>
        )
    }

}*/
